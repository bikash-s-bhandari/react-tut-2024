

const Header = (props) => {

    // const {title,content}=props;
 
  return (
    <div>
        <div>{props.title}</div>
        <div>{props.content}</div>
     
   
  </div>
  );
};

export default Header;
